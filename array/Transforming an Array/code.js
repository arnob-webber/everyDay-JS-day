let numbers = [1, 2, 3, 4];

// Using the map method to create a new array with each number doubled
let doubled = numbers.map(function(number) {
    return number * 2;
});

console.log(doubled); // Output: [2, 4, 6, 8]
