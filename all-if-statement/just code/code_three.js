// let minitues = 2;

// function minitesToHour(minitues) {
//   console.log(minitues * 60);
// }

// minitesToHour(minitues);
let superMan = ["he can fly", "he can use laser", "he is steal"];
var supermanFly 

if (superMan.length > 0) {
 supermanFly = 500;
}

let batmanFly = 20
// let supermanFly = superMan.length > 0;
// let batmanFly = 1;

function superHero(supermanFly, batmanFly) {
  if (supermanFly > batmanFly) {
    console.log("super man is the op hero");
  } else {
    console.log("batman have no power just money");
  }
}
superHero(supermanFly, batmanFly);
